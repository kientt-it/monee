"use client";

import { useState, useTransition } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Building2, CreditCard, Landmark, Plus, WalletCards, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { createAccountAction } from "@/features/accounts/actions/create-account";
import { accountSchema, type AccountInput } from "@/features/accounts/schemas/account.schema";
import type { AccountRecord } from "@/features/accounts/queries/get-accounts";
import { formatCurrency } from "@/lib/utils";

const typeLabels: Record<AccountRecord["type"], string> = { cash: "Tiền mặt", bank: "Ngân hàng", ewallet: "Ví điện tử", credit_card: "Thẻ tín dụng", saving: "Tiết kiệm", investment: "Đầu tư", other: "Khác" };
const typeIcons = { cash: WalletCards, bank: Landmark, ewallet: WalletCards, credit_card: CreditCard, saving: Building2, investment: Building2, other: WalletCards };

const demoAccounts: AccountRecord[] = [
  { id: "demo-cash", name: "Tiền mặt", type: "cash", current_balance: 1250000, currency: "VND", color: "#efaa47", is_archived: false, include_in_total: true },
  { id: "demo-bank", name: "Vietcombank", type: "bank", current_balance: 15700000, currency: "VND", color: "#087f5b", is_archived: false, include_in_total: true },
  { id: "demo-wallet", name: "Momo", type: "ewallet", current_balance: 850000, currency: "VND", color: "#a477b9", is_archived: false, include_in_total: true },
  { id: "demo-saving", name: "Tiết kiệm", type: "saving", current_balance: 35000000, currency: "VND", color: "#8795c4", is_archived: false, include_in_total: true },
];

export function AccountsView({ accounts, configured }: { accounts: AccountRecord[]; configured: boolean }) {
  const [open, setOpen] = useState(false); const [isPending, startTransition] = useTransition(); const [feedback, setFeedback] = useState("");
  const visibleAccounts = configured ? accounts : demoAccounts;
  const total = visibleAccounts.reduce((sum, account) => sum + account.current_balance, 0);
  const form = useForm<AccountInput>({ resolver: zodResolver(accountSchema), defaultValues: { name: "", type: "cash", initialBalance: 0, currency: "VND", includeInTotal: true } });
  function submit(values: AccountInput) { setFeedback(""); startTransition(async () => { const result = await createAccountAction(values); if (!result.ok) { setFeedback(result.error === "SUPABASE_NOT_CONFIGURED" ? "Hãy cấu hình Supabase trước khi lưu dữ liệu thật." : result.error); return; } form.reset(); setOpen(false); }); }
  return <main className="min-h-screen bg-[var(--background)] px-4 pb-28 pt-5 sm:px-6 md:px-10 md:pb-12 md:pt-8"><div className="mx-auto max-w-[900px]"><header className="flex items-center justify-between"><div><p className="text-sm text-[var(--muted)]">Không gian của bạn</p><h1 className="mt-1 text-2xl font-bold tracking-tight md:text-3xl">Tài khoản</h1></div><Button size="sm" onClick={() => { setFeedback(""); setOpen(true); }}><Plus size={16} /> Thêm tài khoản</Button></header>{!configured && <div className="mt-5 rounded-2xl border border-dashed border-[var(--border)] bg-[var(--surface-muted)] px-4 py-3 text-sm text-[var(--muted)]">Bản xem trước · các số dư dưới đây là dữ liệu minh họa, chưa được lưu.</div>}<Card className="mt-6 overflow-hidden bg-[var(--brand)] text-white"><CardContent className="p-6"><p className="text-sm text-white/75">Tổng số dư</p><p className="mt-2 text-3xl font-bold tabular-nums">{formatCurrency(total)}</p><p className="mt-2 text-xs text-white/70">{visibleAccounts.length} tài khoản đang hoạt động</p></CardContent></Card><section className="mt-7"><div className="mb-3 flex items-center justify-between"><h2 className="text-base font-semibold">Tài khoản của bạn</h2><span className="text-xs text-[var(--muted)]">Cập nhật theo số dư hiện tại</span></div>{configured && visibleAccounts.length === 0 ? <Card><CardContent className="p-8 text-center"><WalletCards className="mx-auto text-[var(--brand)]" size={30} /><p className="mt-3 font-semibold">Chưa có tài khoản nào</p><p className="mt-1 text-sm text-[var(--muted)]">Tạo tài khoản đầu tiên để bắt đầu theo dõi dòng tiền.</p><Button className="mt-5" onClick={() => setOpen(true)}><Plus size={16} /> Tạo tài khoản đầu tiên</Button></CardContent></Card> : <div className="grid gap-3 sm:grid-cols-2">{visibleAccounts.map((account) => { const Icon = typeIcons[account.type]; return <Card key={account.id} className="p-4"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl text-white" style={{ background: account.color ?? "var(--brand)" }}><Icon size={19} /></div><div className="min-w-0 flex-1"><p className="truncate font-semibold">{account.name}</p><p className="mt-0.5 text-xs text-[var(--muted)]">{typeLabels[account.type]}</p></div><p className="font-bold tabular-nums">{formatCurrency(account.current_balance)}</p></div></Card>; })}</div>}</section></div>{open && <div className="fixed inset-0 z-40 flex items-end justify-center bg-black/35 p-0 sm:items-center sm:p-4" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setOpen(false); }}><section role="dialog" aria-modal="true" aria-labelledby="account-dialog-title" className="w-full max-w-lg rounded-t-[24px] bg-[var(--surface)] p-5 shadow-2xl sm:rounded-[24px]"><div className="mb-5 flex items-center justify-between"><div><h2 id="account-dialog-title" className="text-lg font-bold">Thêm tài khoản</h2><p className="mt-1 text-sm text-[var(--muted)]">Số dư ban đầu chỉ dùng một lần.</p></div><button aria-label="Đóng" onClick={() => setOpen(false)} className="flex h-10 w-10 items-center justify-center rounded-full hover:bg-[var(--surface-muted)]"><X size={19} /></button></div><form onSubmit={form.handleSubmit(submit)} className="space-y-4"><label className="block text-sm font-semibold">Tên tài khoản<input {...form.register("name")} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 text-base" placeholder="Ví dụ: Techcombank" />{form.formState.errors.name && <span className="mt-1 block text-xs text-[var(--danger)]">{form.formState.errors.name.message}</span>}</label><label className="block text-sm font-semibold">Loại tài khoản<select {...form.register("type")} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 text-base">{Object.entries(typeLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="block text-sm font-semibold">Số dư ban đầu<input {...form.register("initialBalance", { valueAsNumber: true })} type="number" min="0" step="1" inputMode="numeric" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 text-base" placeholder="0" />{form.formState.errors.initialBalance && <span className="mt-1 block text-xs text-[var(--danger)]">{form.formState.errors.initialBalance.message}</span>}</label>{feedback && <p role="alert" className="rounded-xl bg-[#fff0ed] px-3 py-2 text-sm text-[var(--danger)]">{feedback}</p>}<Button type="submit" className="w-full" disabled={isPending}>{isPending ? "Đang lưu…" : "Lưu tài khoản"}</Button></form></section></div>}</main>;
}
